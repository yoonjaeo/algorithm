'''
보석의 형태와 보석을 지닌 돌이 주어짐. 
돌에서 보석찾기 
a != A
'''
import re
def numJewelsInStones(self, jewels: str, stones: str) -> int:
    answer = 0 
    for jewel in jewels:
        answer += len(re.findall(jewel, stones))
        # print(re.findall(jewel, stones))
    return answer